const InputBar = ({ message }: any) => {
  return (
    <div className="flex flex-row justify-center items-center bg-white rounded-md w-fit px-8 py-1 font-semibold">
      {message}
    </div>
  )
}

export default InputBar
